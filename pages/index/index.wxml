<view class="container">
  <image src="{{src}}"></image>
  <text>{{name}}</text>
  <button type="primary" open-type="getUserInfo" bindgetuserinfo="getMyInfo">点击获取头像和昵称</button>
  <!-- open-type="getUserInfo" 获取用户信息，可以从bindgetuserinfo回调中获取到用户信息 -->
  <!-- bindgetuserinfo="getMyInfo"用户点击该按钮时，会返回获取到的用户信息，回调的detail数据与wx.getUserInfo返回的一致，open-type="getUserInfo"时有效 -->
</view>
